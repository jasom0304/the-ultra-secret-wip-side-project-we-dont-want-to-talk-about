/**
 * API Server - Serveur HTTP REST pour contrôler PipeliNostr
 * Sera implémenté dans les prochaines phases
 *
 * Note: Pour les webhooks entrants, voir webhook-server.ts
 */

export class ApiServer {
  // TODO: Implement HTTP API server for:
  // - GET /api/status - Health check
  // - GET /api/workflows - List workflows
  // - POST /api/workflows/:id/trigger - Trigger a workflow
  // - GET /api/stats - Get statistics
  // - GET /api/handlers - List active handlers
  // - POST /api/pollers/:id/trigger - Trigger a poller
  // - POST /api/schedules/:id/trigger - Trigger a schedule
}
